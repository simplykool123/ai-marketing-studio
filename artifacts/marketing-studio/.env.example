# ─── Dev Server ──────────────────────────────────────────────────────────────
# Port for the Vite dev server (default: 5173)
PORT=5173

# Base path for the app — use "/" locally (Replit sets this automatically)
BASE_PATH=/

# Port the Express API server is listening on (for Vite's local proxy)
API_PORT=8080
